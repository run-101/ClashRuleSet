docker run -d \
--name subconverter-extended \
-p 25500:25500 \
-v /Volumes/UrineBag/Docker/subconverter-extended/base:/base \
--restart unless-stopped \
aethersailor/subconverter-extended:latest

docker run -d \
--restart=always \
-e SUB_STORE_FRONTEND_BACKEND_PATH=/sub-store \
-p 25600:3001 \
-v /Volumes/UrineBag/Docker/sub-store:/opt/app/data \
--name sub-store \
xream/sub-store:latest